import { useCreateComment } from "@lens-protocol/react-web";

const CreateComment = () => {
  /* 
    const {excute,error,loading} = useCreateComment({}) */

  return <div>CreateComment</div>;
};

export default CreateComment;
